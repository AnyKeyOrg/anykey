<div class="report-row <%= cycle "odd", "even", name: "row-cycler" %> flexbox vertical stretch" data-url="<%= edit_affiliate_path(affiliate, back: request.fullpath) %>">
  <div class="flexbox horizontal center wrap justify-space-between">
    <div class="row-title"><%= affiliate.name %></div>
    <div class="row-detail"><%= affiliate.title %></div>
  </div>
</div>