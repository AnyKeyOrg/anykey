<div class="flexbox horizontal center wrap justify-space-between">
  <div class="panel-title">
    Verification Review
  </div>
  <div class="panel-descriptor">
    Requested on <%= l(@verification.requested_on, format: "%b. %-d, %Y · %-l:%M%P").upcase %>
  </div>
</div>
<%= render(partial: "verifications/identity") %>
<%= render(partial: "verifications/request_materials") %>
<div class="verification-buttons flexbox horizontal-reverse wrap-reverse center">
  <%= link_to("Verify", verify_eligibility_verification_path(@verification, back: request.fullpath), class: 'verify-button button') %>
  <%= link_to("Deny", deny_eligibility_verification_path(@verification, back: request.fullpath), class: 'deny-button button') %>
  <%= link_to("Ignore", ignore_verification_path(@verification), method: :post, class: 'ignore-button button') %>
  <%= link_to('cancel', params[:back].present? ? params[:back] : verifications_path, class: "stealth" ) %>
</div>