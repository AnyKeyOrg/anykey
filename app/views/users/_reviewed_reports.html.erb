<hr class="spacer">
<div class="flexbox horizontal end wrap-reverse justify-space-between">
  <div class="user-item flush-top flexbox vertical stretch flex-1">
    <div class="item-title large flexbox horizontal center"><span class="icon">&#xf071;</span> Reviewed Reports <%= @user.reviewed_reports.present? ? "(#{@user.reviewed_reports.size})" : ''%></div>
    <% if @user.reviewed_reports.present? %>
    <div class="item-list"><%= render(partial: "reports/row", collection: @user.reviewed_reports.last(3), as: :report) %></div>
    <% else %>
    <div class="item-text large empty">No reports reviewed.</div>
    <% end %>
  </div>
</div>